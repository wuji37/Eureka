spring:
  application:
    name: userservice
  profiles:
    active: dev # 开发环境，命名空间名字
  cloud:
    nacos:
      server-addr: localhost:8848 # Nacos 地址
      config:
        file-extension: yaml # 文件后缀名